{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
        <body class="bc-index">
        {% include 'components/layout/navbar_vertical.html.twig' %}
    <div class="wrap edit-wrap">
    <h3 class="search-header">Registracija:</h3>
    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email, {'label': 'El. Paštas'}) }}
        {{ form_row(registrationForm.plainPassword, {'label': 'Slaptažodis'}) }}
        {{ form_row(registrationForm.agreeTerms, {'label': 'Sutinku su taisyklėmis'}) }}

        <button id="user_info_save">Registruotis</button>
    {{ form_end(registrationForm) }}
    </div>
        </body>
{% endblock %}
